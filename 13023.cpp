#include <iostream>
#include <queue>
using namespace std;
typedef pair<int, int> pii;
int main(){
	int n, m;
	priority_queue<pii> pq;
	for(int i = 0 ; i < m ; i ++){
		int x, y;
		cin >> x >> y;
		pq.push({-})
	} 
	return 0;
}
